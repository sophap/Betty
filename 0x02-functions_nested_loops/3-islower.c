#include "main.h"

/**
 * _islower - Entry point
 *
 * Return: Always 0 (Success)
 */
int _islower(int c)
{
	int r;
	c = {'o', 108, 'c'}

	r = _islower(c);
	_putchar(r + '0');
	_putchar('\n');
	return (0);
}
